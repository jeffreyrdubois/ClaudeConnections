# Cloudflare Tunnel token
# Get this from: Cloudflare Zero Trust → Networks → Tunnels → your tunnel
# → Configure → click "Docker" → copy the token from the run command shown
CLOUDFLARE_TUNNEL_TOKEN=
